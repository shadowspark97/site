# Development environment overrides for Create React App
# Allow access from other devices on the same Wi-Fi network
HOST=0.0.0.0

# (Optional) Uncomment to force a specific port if 3000 is busy
PORT=3000

# (Optional) If you later add client-side routing with BrowserRouter and a basename,
# you do NOT set PUBLIC_URL here for dev; CRA injects it on build from package.json homepage.
# PUBLIC_URL=

# Enable/disable fast refresh (default is true)
# FAST_REFRESH=true
